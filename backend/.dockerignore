# Docker reads this from the build CONTEXT root, which is backend/
# (`docker build -f backend/deploy/Dockerfile backend`) — not from deploy/.
#
# The image builds its own data at build time, so none of data/ is copied in.
data/
venv/
tests/
bench/
.pytest_cache/
__pycache__/
*.egg-info/
.DS_Store
